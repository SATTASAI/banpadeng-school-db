# อัปเดตระบบนำเข้านักเรียนครั้งละ 20 คน

เลือกไฟล์ Excel/CSV ครั้งเดียว ระบบส่งข้อมูลตามลำดับครั้งละไม่เกิน 20 รายการจนจบไฟล์
814 รายการ = 40 ชุด ชุดละ 20 รายการ + ชุดสุดท้าย 14 รายการ รวม 41 ชุด
ไม่มีการจำกัดยอดรวมไว้ที่ 814 รายการ

## วิธีอัปเดตระบบเดิม

นำไฟล์ต่อไปนี้จาก ZIP ไปแทนที่/เพิ่มในตำแหน่งเดิมของโปรเจกต์ GitHub แล้ว deploy ตามวิธีเดิม:

1. `public/students.html` — แทนที่ไฟล์เดิม
2. `public/js/student-import-batches.js` — เพิ่มไฟล์ใหม่
3. `src/index.js` — แทนที่ไฟล์เดิม

ต้องอัปเดตทั้งสามไฟล์ให้เป็นเวอร์ชันเดียวกัน จากนั้นเปิดหน้านักเรียนและกด Ctrl+F5 เพื่อโหลดหน้าใหม่
การแก้ไขครั้งนี้ไม่ต้องสร้างฐานข้อมูลใหม่ และไม่ได้แก้ไฟล์ schema.sql
ไม่ต้องรัน schema.sql ทั้งไฟล์ซ้ำกับฐานข้อมูลที่ใช้งานอยู่

## วิธีนำเข้า

1. เข้าระบบด้วยสิทธิ์ผู้ดูแลระบบ ผู้บริหาร หรือเจ้าหน้าที่ธุรการ
2. ไปที่นักเรียน → นำเข้าจาก Excel/CSV → เลือกไฟล์ทั้งชุด
3. ตรวจแถวหัวตารางและการจับคู่คอลัมน์จากตัวอย่างให้ถูกต้อง
   ถ้าไฟล์แยกชื่อ ให้เลือก “ชื่อ-นามสกุล” เป็น “ไม่นำเข้า” แล้วจับคู่คำนำหน้า ชื่อ และนามสกุลแยกกัน
   ตรวจเลขประจำตัวนักเรียนว่าตรงกับรหัสในโรงเรียน ไม่ใช่เลขบัตรประชาชนหรือคอลัมน์ว่าง
4. กดยืนยันนำเข้าครั้งเดียว ระบบทยอยนำเข้าทีละ 20 รายการ
5. เปิดหน้าเว็บไว้จนความคืบหน้าครบ และตรวจยอดเพิ่มใหม่/อัปเดต/ข้าม
   ความคืบหน้าครบหมายถึงประมวลผลครบ ไม่ได้หมายความว่าทุกแถวถูกบันทึก หากมีรายการข้ามให้ตรวจเหตุผล

## หากหยุดกลางทาง

- กด “นำเข้าต่อจากชุดที่ค้าง” ระบบเริ่มจากชุดที่ยังไม่ได้รับการยืนยัน ไม่ส่งชุดก่อนหน้าที่ได้รับการยืนยันแล้วซ้ำ
- ระหว่างนำเข้า ระบบล็อกไฟล์และการจับคู่คอลัมน์เพื่อให้แต่ละชุดใช้ข้อมูลเดียวกัน
- ความคืบหน้าเก็บเฉพาะในหน้าที่เปิดอยู่ การปิด/รีเฟรช/ออกจากหน้านี้จะล้างจุดที่ค้าง
- หากปิดหน้าไปแล้ว ให้เลือกไฟล์เดิมและจับคู่เดิมเพื่อนำเข้าใหม่ ระบบอัปเดตนักเรียนตามเลขประจำตัวเดิมแทนการสร้างซ้ำ
- หากการเชื่อมต่อขาดหลังเซิร์ฟเวอร์บันทึกแล้ว การลองชุดนั้นใหม่อาจนับเป็น “อัปเดต” ยอดแสดงอ้างอิงผลตอบกลับที่ได้รับ
- เลข “รายการข้อมูลที่” ในรายงานข้ามนับจากข้อมูลหลังหัวตารางและตัดแถวว่างแล้ว ไม่ใช่เลขแถว Excel เสมอไป

## หากแจ้งว่าฐานข้อมูลขาดคอลัมน์

ปัญหานี้เป็นโครงสร้างฐานข้อมูลเดิม ไม่ใช่ขนาดชุดนำเข้า ผู้ดูแลควรตรวจใน D1 Console ด้วย:

```sql
PRAGMA table_info(students);
```

ตรวจว่ามี national_id, name_prefix, first_name, last_name, birth_date ครบหรือไม่
ถ้าขาด ให้รันเฉพาะคำสั่งของคอลัมน์ที่ยังไม่มี ห้ามรันคำสั่งของคอลัมน์ที่มีแล้ว:

```sql
ALTER TABLE students ADD COLUMN national_id TEXT;
ALTER TABLE students ADD COLUMN name_prefix TEXT;
ALTER TABLE students ADD COLUMN first_name TEXT;
ALTER TABLE students ADD COLUMN last_name TEXT;
ALTER TABLE students ADD COLUMN birth_date TEXT;
```

## รายละเอียดสำหรับผู้ดูแล

- API ปฏิเสธคำขอที่มีเกิน 20 รายการ เพื่อไม่ให้หน้าเว็บรุ่นเก่าส่งทั้งไฟล์ในครั้งเดียว
- แต่ละคำขอตรวจข้อมูลเดิมหนึ่งครั้ง แล้วบันทึกไม่เกิน 20 คำสั่ง รวมการตรวจบัญชีเป็นไม่เกิน 22 คำสั่ง SQL
- ใช้ INSERT ... ON CONFLICT(student_code) DO UPDATE ป้องกันการเพิ่มเลขประจำตัวเดิมซ้ำเมื่อส่งชุดเดิมอีกครั้ง
- คำสั่งบันทึกแต่ละชุดใช้ D1 batch เพื่อให้ล้มเหลวแล้ว rollback ทั้งชุด ตามเอกสาร https://developers.cloudflare.com/d1/worker-api/d1-database/#batch
- ไม่เปลี่ยนสิทธิ์การนำเข้าและไม่แก้ระบบจับคู่คอลัมน์เดิมในการปรับรุ่นนี้
- เช่นเดียวกับรุ่นเดิม การอัปเดตแถวที่มีเลขประจำตัวตรงกันจะแทนค่าฟิลด์ที่นำเข้า จึงต้องตรวจคอลัมน์ก่อนยืนยัน

## ผลตรวจสอบก่อนส่งมอบ

ทดสอบด้วยข้อมูลสังเคราะห์และ SQLite ภายในเครื่อง ไม่ใช่ข้อมูลนักเรียนจริงหรือ D1 ที่ใช้งานจริง:

- 814 รายการ: 41 คำขอ ได้ข้อมูล 814 รายการ
- ส่งไฟล์เดิมซ้ำ: จำนวนเลขประจำตัวไม่เพิ่มซ้ำ
- จำลอง SQL ผิดพลาดกลางชุดที่ 5: ย้อนกลับชุดนั้น เหลือ 80 รายการ แล้วนำเข้าต่อจนครบ 814
- จำลองบันทึกแล้วผลตอบกลับหาย: ลองใหม่ได้และไม่สร้างเลขประจำตัวซ้ำ
- ตรวจเลขรายการข้าม สิทธิ์ผู้ใช้ การปฏิเสธชุดเกิน 20 และข้อความฐานข้อมูลขาดคอลัมน์
- ทดสอบตัวจัดการปุ่มจริงในหน้าเว็บผ่าน DOM จำลอง: ล็อกช่องระหว่างนำเข้า แสดงปุ่มลองต่อ และแสดง 814/814 เมื่อเสร็จ

ยังไม่ได้ทดสอบกับไฟล์ DMC จริง เพราะยังไม่ได้รับไฟล์ดังกล่าว
